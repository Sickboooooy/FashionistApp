node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz.local/

# Secrets — never commit real env files
.env
.env.local
.env.*.local

# Editor/tool history backups (regenerados localmente)
.history/
.snapshots/
